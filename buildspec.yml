version: 0.2
phases:
  install:
    commands:
      - echo "Installing..."
      - npm install
  build:
    commands:
      - tar --exclude .git -cjv -f bundle.tar.gz .
artifacts:
  files:
    - 'bundle.tar.gz'
